# Data Navigator · 功能使用说明

**适用版本**：v0.2+（与 `doc/doc.md` / 主程序 `main-version.py` 同步）  
**面向对象**：初次使用的用户、业务分析人员、教学场景用户  
**最后更新**：2026-05-12

> 本文档随功能开发进度持续更新。每新增一个功能模块，必须在对应章节补充说明。

---

## 目录

1. [软件整体介绍](#1-软件整体介绍)
2. [主界面说明](#2-主界面说明)
3. [文件菜单](#3-文件菜单)
4. [数据获取菜单](#4-数据获取菜单)
5. [数据预处理菜单](#5-数据预处理菜单)
6. [描述性统计分析菜单](#6-描述性统计分析菜单)（首版已上线）
7. [数据挖掘菜单](#7-数据挖掘菜单)（首版已上线）
8. [数据可视化菜单](#8-数据可视化菜单)（首版已上线，持续完善）
9. [AI 分析菜单](#9-ai-分析菜单)（已上线：建议生成与步骤执行）
10. [帮助菜单](#10-帮助菜单)（基础入口已上线）
11. [通用操作规则](#11-通用操作规则)
12. [开发维护约定](#12-开发维护约定)
13. [版本能力核对摘要](#13-版本能力核对摘要)
14. [10 分钟快速上手](#14-10-分钟快速上手)

---

## 1. 软件整体介绍

本软件是一款面向数据分析人员的桌面端工具，提供从数据导入、清洗、分析到可视化、报告输出的完整工作流。

**核心设计理念**：
- 以"用户在表格里看到的数据"为准进行所有分析和处理
- 每次操作产出新结果表，不覆盖原始数据，便于对比和回退
- 界面操作为主，无需编写代码

**功能模块状态总览**：

| 功能模块 | 状态 |
|----------|------|
| 数据导入 | ✅ 已完成 |
| 数据获取（抽样） | ✅ 已完成 |
| 数据预处理 | ✅ 已完成 |
| 描述性统计分析 | ✅ 首版已完成（含假设检验） |
| 数据挖掘 | ✅ 首版已完成 |
| 数据可视化 | ✅ 首版已完成（工作台 + 跨模块快捷图） |
| AI 分析 | ✅ 已上线（建议生成、自动执行、选择性执行、模型切换） |
| 数据报告生成 | ✅ V1 已上线（配置模板 + 预览 + HTML 导出） |
| 打印 | ✅ 已实现打印预览（大表默认前 1000 行） |

---

## 2. 主界面说明

软件启动后，主界面由以下区域组成：

### 2.1 菜单栏（顶部）

从左到右依次为：文件 / 数据获取 / 数据预处理 / 描述性统计分析 / 数据挖掘 / 数据可视化 / AI分析 / 帮助 / **查看结果窗口** / **数据报告** / 设置。
（「查看结果窗口」已从描述性统计分析子菜单迁移至主菜单栏，便于随时打开统一结果窗口。）

### 2.2 工具图标栏（菜单栏下方）

常用操作的快捷图标，从左到右依次为：

| 图标 | 功能 |
|------|------|
| 文档图标 | 保存当前文件 |
| 数据模型图标 | 打开表关系管理 |
| 结果窗口图标 | 打开统一结果窗口 |
| 数据报告图标 | 打开数据报告配置 |
| 设置图标 | 打开设置窗口 |
| 统计图标 | 打开图表工作台 |
| 搜索图标 | 搜索（占位，开发中） |
| AI 图标 | 打开 AI 助手侧栏 |

### 2.2.1 左侧变量侧边栏（新增）

- 主表左侧新增可伸缩变量侧边栏，包含：变量总览、列数统计、搜索框、变量列表。
- 变量列表项格式：`变量名 [标签] | 类型 | 角色`，点击后可在主表中定位并高亮对应列。
- 支持收起/展开；搜索支持按变量名、标签、类型、角色关键字过滤。
- 主表切换、列结构变化、变量角色/标签/类型变化后，侧边栏会自动同步刷新。

### 2.2.2 角色图标（新增）

- 已为变量角色增加图标标识，并显示在“变量名左侧”：
  - `x`：特征变量
  - `y`：目标变量
  - `outcome`：结果变量
  - `none`：未指定
  - `category`：分组变量
  - `id`：ID变量
  - `time`：时间变量
  - `ignore`：忽略变量
- 图标会同步显示在：
  - 左侧变量侧边栏
  - 变量视图（变量名列）
  - 各分析/挖掘/可视化对话框中的列名拖拽列表、变量下拉框（如假设检验）
- 主页面主表格的数据单元格显示不受影响（不会在主表单元格额外显示角色图标）。

### 2.2.3 拖拽自动赋角色（仅未指定）

- 在描述统计、频率分布、相关性、分类、回归、聚类、降维、图表工作台等拖拽场景中：
  - 列角色为“未指定”时，拖入槽位后会自动赋对应角色；
  - 列已有角色时，不会被覆盖。
- 常见映射：
  - X/特征槽位 -> `特征变量`
  - y/目标槽位 -> `目标变量`
  - 分组/分面槽位 -> `分组变量`
- 算法或流程自动生成的结果列（如回归预测值、降维结果列）默认标记为 `结果变量`。

### 2.3 数据表格区（中部）

- 显示当前打开的数据表
- 支持直接在单元格内输入/编辑数据
- 支持右键菜单（插入行/列、删除行/列、复制/粘贴/剪切/清空、以该行为列名）
- 双击列头可重命名列
- 若列在 **变量视图** 中填写了「**变量标签**」，将鼠标悬停在列头名称上可查看该标签（列名仍为变量名）。

### 2.4 表格切换栏（表格区下方）

- 每张表对应一个按钮，点击切换
- 蓝色高亮按钮 = 当前激活表
- 右键按钮可重命名、删除表、**切换到变量视图**
- **变量视图**中可设置每列的变量名、类型、小数位数与**变量标签**（人类可读说明）；标签会随 `.glc` 保存；在统计、挖掘等对话框的变量列表中显示为「变量名 [标签]」，过长时悬停可看完整内容。
- 最右侧「+」按钮新建空表（固定贴在切换栏右缘可视区内；当表数量很多时，表名按钮会自动变窄，避免把「+」挤出窗口）

### 2.5 状态信息栏（最底部）

从左到右显示：当前数据大小 `[行, 列]` / 当前选中位置 / 当前单元格值 / 文件保存路径。

---

## 3. 文件菜单

### 3.1 打开

- **作用**：导入外部数据文件到新表。
- **支持格式**：`.glc`（软件专用格式）/ `.csv` / `.xlsx` / `.xls` / `.json` / `.parquet` / `.txt`
- **当前交互**：点击后直接进入资源管理器选择文件，不再弹出导入向导；文件类型由后缀自动识别。
- **结构调整入口**：
  - 转置：使用「数据获取 -> 转置当前表」
  - 以某行为列名：在主表该行右键，选择「以该行为列名」

### 3.2 保存

- **作用**：保存当前工作区到 `.glc` 文件（含所有表格、列类型信息）。
- **首次保存**：自动保存到默认路径（可在"设置"中修改）。

### 3.3 另存为

- **作用**：将当前数据另存为指定格式和路径。
- **支持格式**：`.glc` / `.xlsx` / `.csv`

### 3.4 打印

- **作用**：打印当前表格。
- **当前版本**：已支持打印预览。
- **行为说明**：
  - 自动按当前激活表生成打印预览页面。
  - 为避免超大表直接打印卡顿，若数据超过 1000 行，默认仅打印前 1000 行，并在预览页提示。

### 3.5 退出

- **作用**：关闭软件。若"设置"中开启了退出提醒，会弹出确认对话框。

---

## 4. 数据获取菜单

数据获取菜单提供多种导入方式与抽样能力，导入后列名与数据形状由模型统一同步（见细节实现文档）。

### 4.1 导入类（文件与剪贴板）

| 菜单项 | 说明 |
|--------|------|
| **导入 JSON** | 选择 JSON 文件导入为新表；支持数组/对象自动展平为表格，多编码尝试（UTF-8/GBK）。 |
| **导入 Parquet** | 选择 `.parquet` 文件导入（需安装 pyarrow 或 fastparquet）。 |
| **导入 TXT** | 选择文本文件，自动推断分隔符后导入。 |
| **从剪贴板导入** | 将剪贴板内容按分隔符解析为表格，表名自动为「剪贴板-时间戳」。 |
| **批量导入文件夹** | 选择目录 + 文件类型（CSV/Excel/JSON/Parquet/TXT），同类型文件按行合并为一张表；可选添加「来源文件名」列。 |
| **导入预览** | 先选择文件类型与文件，预览前 20 行及列类型后再确认导入。 |
| **增量导入** | 基于当前表作为基表，选择增量文件与「主键列」或「时间列」，按去重或时间增量追加后生成新表「原表名-增量导入」。 |
| **转置当前表** | 将当前激活表按行列互换，按标准数据打开流程生成新表（不覆盖原表）。 |
| **导入 SQLite** | 选择 `.db`/`.sqlite` 文件，列出库内表供选择，可设行数限制，导入为新表。 |
| **导入历史** | 列出最近 50 条导入记录（含文件/目录/SQLite），选择一条可一键复用该次导入（文件/目录需仍存在）。历史持久化在 `MyFile/.import_history.json`。 |

以上导入均通过统一写表流程，并触发**列名与数据形状同步**（见细节实现文档 3.2、7.3）。

### 4.2 选择样本

- **作用**：从当前表中抽取子样本，结果输出为新表。
- **抽样方式**：

| 方式 | 说明 |
|------|------|
| 全选 | 完整复制当前表数据 |
| 概率抽样 | 随机/分层/系统抽样 |
| 非概率抽样 | 按条件表达式筛选 |

#### 概率抽样子选项

**随机抽样**：
- 大约抽样：按比例（如 0.5 = 抽 50%）
- 精准抽样：指定截止行号 + 样本数量
- 可选"重复抽样"（允许同一行被多次抽到）

**分层抽样**：
- 指定分层特征列，在每层内按比例/数量抽取
- 参数同随机抽样

**系统抽样**：
- 指定抽样间距、起始位置、样本数量
- 按等间隔抽取

#### 非概率抽样（条件筛选）

- 通过表达式筛选行，如 `年龄 > 18`
- 支持逻辑运算符（AND/OR/NOT）和比较运算符

---

## 5. 数据预处理菜单

> **通用规则**：
> - 所有操作以"用户可见数据"为准（即表格里显示的内容）
> - 每次操作产出新结果表，命名规则：`{原表名}-{操作名}`
> - 列名输入框：逗号分隔多列，**留空 = 自动处理全部适用列**（**缺失值删除 / 缺失值填充 / 异常值处理**为独立窗口中的双列表选列，见 5.2～5.4；左侧列顺序与数据表一致，**移回左侧**时仍按原列顺序排列）

### 5.1 缺失值统计

- **作用**：统计每列的缺失值数量和缺失率，输出统计结果表。
- **建议**：在做填充/删除前先执行，了解数据质量。
- **无需任何参数输入**。
- **可选图表**：已支持运行后选择追加「缺失值柱状图」或「缺失值热力图」到统一结果窗口（不影响结果表生成）。

### 5.2 缺失值填充

- **作用**：将空值填补为可用值。
- **界面**：单独「缺失值填充」窗口，在同一窗口内设置 **填充策略**、**固定填充值**（仅 `fixed` 时必填）、以及 **待选择特征 / 目标特征** 双列表（右侧为空 = 全部列）。

| 策略 | 说明 | 适用列 |
|------|------|--------|
| `mean` | 均值填充 | 数值列 |
| `median` | 中位数填充 | 数值列 |
| `mode` | 众数填充 | 所有列 |
| `fixed` | 固定值填充 | 所有列 |
| `ffill` | 前向填充（用上一行值） | 所有列 |
| `bfill` | 后向填充（用下一行值） | 所有列 |

### 5.3 缺失值删除

- **作用**：删除含缺失值的行或列，并可限定在部分字段上判断缺失。
- **界面**：单独「缺失值删除」窗口，在同一窗口内设置：
  1. **删除维度**：`行` / `列`
  2. **删除条件**：`any`（有任一缺失就删）/ `all`（全部缺失才删）
  3. **作用列**：左侧 **待选择特征**、右侧 **目标特征**，支持拖拽与中间按钮移动；**右侧为空** = 按全部列参与缺失判断。

### 5.4 异常值处理

- **作用**：识别并处理数据中的离群点。
- **界面**：单独「异常值处理」窗口。**左侧待选列**仅包含「至少有一个可转为数值的单元格」的列（与引擎一致）；在同一窗口内设置 **检测方法**、**处理策略**、**Z-Score 阈值**（仅 `zscore` 时启用），以及 **目标特征** 双列表（右侧为空 = 上述全部数值列）。
- **执行后**：先弹出每列异常值数量的统计信息，再输出处理结果表。

| 检测方法 | 说明 |
|----------|------|
| `iqr` | 四分位距法（推荐，不受极值影响） |
| `zscore` | Z 分数法（适合正态分布数据） |
| `3sigma` | 三倍标准差法 |

| 处理策略 | 说明 |
|----------|------|
| `nan` | 将异常值置为空 |
| `clip` | 将异常值截断到边界值 |
| `drop` | 删除含异常值的整行 |

### 5.5 数据类型转换

- **作用**：将指定列转换为目标数据类型。
- **步骤**：
  1. 输入目标列名（必填）
  2. 选择目标类型

| 目标类型 | 说明 |
|----------|------|
| `整数` | 转为整数，无法转换的变为空 |
| `小数` | 转为浮点数，无法转换的变为空 |
| `字符串` | 转为文本 |
| `日期` | 转为日期格式，无法解析的变为空 |

### 5.6 重复值处理

- **作用**：删除重复行。
- **步骤**：
  1. 输入判定列名（留空=所有列联合判重）
  2. 选择保留策略

| 保留策略 | 说明 |
|----------|------|
| `first` | 保留第一次出现的行 |
| `last` | 保留最后一次出现的行 |
| `False` | 所有重复行都删除 |

### 5.7 字符串清洗

- **作用**：清理文本列中的脏数据。
- **步骤**：
  1. 输入目标列名（留空=全部列）
  2. 输入正则替换模式（留空=不替换）
  3. 输入替换为的内容（默认空串）
  4. 选择是否去除首尾空白

### 5.8 列编码

- **作用**：将分类文本列转换为数值，用于后续建模。
- **步骤**：
  1. 输入目标列名（必填）
  2. 选择编码方法

| 编码方法 | 说明 | 输出 |
|----------|------|------|
| `onehot` | 独热编码 | 每个类别生成一列 0/1 |
| `label` | 标签编码 | 单列整数（0, 1, 2...） |

### 5.9 数据标准化

- **作用**：消除量纲差异，使各列数值在同一尺度。
- **步骤**：
  1. 在双列表窗口中从左侧选择目标数值列（右侧为空=全部数值列）
  2. 选择标准化方法

| 方法 | 说明 | 结果范围 |
|------|------|----------|
| `zscore` | Z 分数标准化 | 均值 0，方差 1 |
| `minmax` | 最小最大归一化 | [0, 1] |

### 5.10 数据归一化

- **作用**：按样本（行）向量长度归一化，常用于文本/向量数据。
- **步骤**：
  1. 在双列表窗口中从左侧选择目标数值列（右侧为空=全部数值列）
  2. 选择归一化范数

| 范数 | 说明 |
|------|------|
| `l2` | 欧氏距离（默认，最常用） |
| `l1` | 曼哈顿距离 |
| `max` | 最大绝对值 |

### 5.11 列运算/派生列

- **作用**：通过表达式计算生成新列。
- **步骤**：
  1. 输入新列名
  2. 输入计算表达式

**表达式示例**：

| 表达式 | 说明 |
|--------|------|
| `A + B` | A 列与 B 列相加 |
| `A * 2` | A 列乘以 2 |
| `A / B` | A 列除以 B 列 |
| `A - B.mean()` | A 列减去 B 列均值 |

---

## 6. 描述性统计分析菜单

> ✅ **已上线（第一版）**：描述统计、频率分布、相关性分析、假设检验。  
> 面向零基础用户：如果你不知道该点哪个功能，先看本节 6.5 的“检验怎么选”。

| 功能 | 解决什么问题 | 典型场景 |
|------|--------------|----------|
| 描述统计 | 快速看数据总体水平和波动 | 先摸清数据概况 |
| 频率分布 | 看某个变量在不同区间的分布情况 | 年龄段、分数段、消费段统计 |
| 相关性分析 | 看多个变量之间是否同向/反向变化 | 销量与广告、学习时长与成绩 |
| 假设检验 | 判断“差异/效应”是否显著，不靠肉眼猜 | A/B 对比、前后对比、多组比较 |

### 6.1 统一结果窗口（核心）

描述性统计分析结果统一显示在 **统一结果窗口**。

- **结构**：标题区 + 滚动结果区 + 工具按钮区。  
- **追加模式**：每次运行会在底部新增结果块，不覆盖历史结果。  
- **关闭行为**：点关闭只是隐藏，历史结果保留。  
- **可选图表**：勾选“可视化选项”后，图表会和表格一起追加。  
- **查看入口**：主菜单栏「查看结果窗口」。

### 6.2 描述统计（拖拽选变量）

- **它是做什么的**：告诉你每个变量的均值、中位数、标准差、四分位数等，先“看全局”再决定下一步。  
- **建议什么时候用**：任何分析前都先跑一次，避免盲目建模。  
- **操作步骤**：
  1. 左侧“可用变量”选择筛选类型（全部/仅数值型/仅分类型）。  
  2. 拖拽变量到中间“已选变量”。  
  3. 在右侧勾选需要的统计指标。  
  4. （可选）勾选附带图表（直方图/箱线图）。  
  5. 点击“运行”，结果会追加到统一结果窗口。  

### 6.3 频率分布（单变量）

- **它是做什么的**：统计每个类别/区间出现了多少次，占比多少。  
- **常见例子**：把“成绩”按区间分箱，看低分、中分、高分人数。  
- **操作步骤**：
  1. 拖拽 1 个变量到“已选变量”。  
  2. 若是数值型，设置分箱方式（等宽/等频）和分箱数。  
  3. 可勾选“显示累计频率”。  
  4. （可选）勾选柱状图或直方图。  
  5. 点击“运行”，频率表和图表追加到结果窗口。  

### 6.4 相关性分析（多变量）

- **它是做什么的**：计算变量两两之间的相关系数，范围通常在 `-1` 到 `1`。  
- **怎么理解结果**：
  - 接近 `1`：同向强相关（一个变大，另一个也常变大）。  
  - 接近 `-1`：反向强相关（一个变大，另一个常变小）。  
  - 接近 `0`：线性相关不明显。  
- **操作步骤**：
  1. 拖拽至少 2 个变量（至少 2 个数值型）。  
  2. 选择方法（Pearson/Spearman/Kendall）。  
  3. （可选）勾选热力图。  
  4. 点击“运行”。  
- **高亮规则**：
  - `>= 0.7`：偏红（强正相关）  
  - `<= -0.7`：偏蓝（强负相关）

### 6.5 假设检验

假设检验用于回答一个关键问题：**你看到的差异，是真差异，还是“碰巧”？**

#### 6.5.1 软件里支持的检验类型

| 检验类型 | 适用于什么情况 | 你需要准备什么数据 |
|----------|----------------|--------------------|
| 单样本 t 检验 | 一组数据均值是否等于某个目标值（μ0） | 1 列数值 + 假设均值 |
| 独立样本 t 检验 | 两组互不重叠样本的均值是否不同 | 2 列数值（分别代表两组） |
| 配对样本 t 检验 | 同一对象“前后两次”或“成对条件”均值是否不同 | 2 列一一对应数值 |
| 单因素 ANOVA | 三组及以上均值是否存在差异 | 1 列数值 + 1 列分组标签 |
| 卡方检验（列联表） | 两个分类变量是否相关 | 2 列分类变量 |
| Mann–Whitney U | 两组独立样本分布差异（非参数） | 2 组数值数据（可由两列或分组形成） |
| Kruskal-Wallis H | 三组及以上分布差异（非参数） | 1 列数值 + 1 列分组标签 |

#### 6.5.2 怎么选检验（最简单判断法）

1. **只有一组数据，拿来和目标值比较** → 选“单样本 t 检验”。  
2. **两组人，互不重叠（例如 A 班和 B 班）** → 选“独立样本 t 检验”。  
3. **同一批人，测了两次（例如培训前后）** → 选“配对样本 t 检验”。  
4. **三组及以上比较（例如三种方案）** → 选“单因素 ANOVA”或 “Kruskal-Wallis”。  
5. **分类变量 vs 分类变量**（如 性别 vs 是否流失）→ 选“卡方检验（列联表）”。  
6. **两组数值明显偏态/异常值较多** → 优先“Mann–Whitney U”。

#### 6.5.3 独立 t 检验的“样本怎么选”

- **样本应互相独立**：同一个人不能同时出现在两组。  
- **两组应代表同一问题下的两个对比条件**：如“方案A”和“方案B”。  
- **每组尽量都有足够样本**：太少会导致结论不稳定。  
- **本软件当前输入方式**：独立 t 检验使用“两列数值”做比较；每列代表一组。  
  - 例：`A组得分` 一列、`B组得分` 一列。  

#### 6.5.4 操作步骤（按界面一步一步）

1. 点击「描述性统计分析 → 假设检验」。  
2. 在“检验类型”选择你的场景。  
3. 选择变量（单样本选变量1；独立/配对选变量1+变量2；ANOVA 选数值变量 + 分组变量）。  
4. 设置参数：  
   - `alpha`（显著性水平，默认 `0.05`）  
   - 单样本时设置 `μ0`  
   - 独立样本时可勾“方差齐性”  
5. 点击“运行”，结果会进入统一结果窗口。  

#### 6.5.5 结果怎么看（零基础版）

- 重点看 `p值` 与 `alpha`：  
  - `p < alpha`：拒绝原假设（可以认为“差异显著”）。  
  - `p >= alpha`：不拒绝原假设（不能证明有显著差异）。  
- 结果窗口会给两块内容：  
  - **检验摘要**：统计量、p值、结论。  
  - **样本信息**：样本量、均值、标准差等，帮助你理解结论来源。  

#### 6.5.6 常见误区（非常重要）

- 误区 1：`p值小` 就代表“效果很大”。  
  - 实际：`p值`只表示“显著性”，不直接表示效果大小。  
- 误区 2：配对数据用独立样本 t 检验。  
  - 实际：前后对比应优先用配对检验。  
- 误区 3：分组很多还用多次 t 检验。  
  - 实际：三组及以上优先用 ANOVA。  

#### 6.5.7 批量单因素检验（新增）

当你要一次性筛查很多变量时，建议使用「**描述性统计分析 -> 批量单因素检验**」：

1. 选择**目标变量**（如 `Churn`）。  
2. 勾选参与比较的**自变量列表**（默认全选，可排除不需要的变量）。  
3. 选择模式：  
   - 自动推荐（默认）：系统按变量类型/分组自动选检验；  
   - 手动统一检验：所有变量按指定检验类型尝试。  
4. （可选）配置输出字段顺序、是否启用多重比较校正、是否添加显著性星号。  
5. 运行后在统一结果窗口查看：  
   - `批量单因素检验 - 总结果`  
   - `批量单因素检验 - 失败/跳过明细`  
   - `批量单因素检验 - 样本明细`

#### 6.5.8 多重比较校正与显著性标记（新增）

- **默认关闭**：避免影响探索性分析速度。  
- 可选校正方法：`Bonferroni`、`FDR(BH)`。  
- 可选显著性标记：`* / ** / ***`。  
- 建议：用于正式报告或论文时，至少开启一种校正方法。

---

## 7. 数据挖掘菜单

> ✅ **首版已上线**（与 `main-version.py` 一致）。
> 分类分析（逻辑回归/决策树/随机森林/K近邻）、**回归分析**（线性回归/Ridge/Lasso/弹性网络/决策树/随机森林）、聚类分析（K-Means/层次聚类/DBSCAN）与降维（PCA/t-SNE）均已开放；运行后先在统一结果窗口显示进度条，完成后替换为结果块；可选评估图表见 8.2 节。

**通用约定**：
- **数据源**：以当前激活表格的「用户可见数据」为输入，与预处理、描述统计一致。
- **变量选择**：支持从「可用变量」拖拽到「已选变量」；分类/回归需选择**特征变量（X）**和**目标变量（y）**，聚类仅选择特征变量。建议先完成缺失值处理或编码，非数值特征需在预处理中做列编码。
- **缺失处理**：运行前会对参与建模的列自动删除含缺失值的行，并在结果中提示删除行数。
- **结果查看**：所有评估指标、混淆矩阵、系数表，以及（在启用时的）图表均追加到**统一结果窗口**（入口：描述性统计分析 → 查看结果窗口）；聚类会额外生成一张带「簇标签」列的新表。

| 功能 | 说明 |
|------|------|
| 分类分析 | 算法可选：逻辑回归/决策树/随机森林/K近邻；**参数区仅显示当前所选算法的专属项**（+ 测试集比例、随机种子、SelectKBest 等公用项）；输出准确率、精确率、召回率、F1、混淆矩阵；树模型附带特征重要性，支持 SelectKBest。可选图表：混淆矩阵热力图、ROC 曲线（仅二分类）、PR 曲线（仅二分类）。 |
| 回归分析 | 算法可选：线性回归/Ridge/Lasso/弹性网络/决策树/随机森林；**参数区仅显示当前所选算法的专属项**（+ 测试集比例、随机种子、SelectKBest 等公用项）；输出 MSE、RMSE、MAE、R²；线性族为系数表（含截距），树模型为特征重要性表。可选图表：预测值 vs 真实值散点图、残差图、预测分布图。 |
| 聚类分析 | 算法可选：K-Means/层次聚类/DBSCAN；**参数区仅显示当前所选算法的专属项**（+ 提示、可选图表）；输出轮廓系数、各簇样本数（DBSCAN 含噪声点统计），并生成带「簇标签」的新结果表。可选图表：二维聚类散点图、肘部法则曲线（仅 K-Means）、轮廓系数随 k 曲线（仅 K-Means）。 |
| 降维分析 | **单一菜单项**，对话框内选择 PCA 或 t-SNE；**参数随方法切换**；输出降维得分表、方差/参数信息。可选图表：前两维散点图、PCA 碎石图（方差累计，仅 PCA）、高维结果散点矩阵（仅 PCA 且维度较高）。 |

### 7.1 分类分析

- **步骤**：
  1. 菜单选择「数据挖掘 → **分类分析**」
  2. 在可用变量中筛选类型（全部/仅数值型/仅分类型），将**特征变量**拖入「已选特征」，将**目标变量**（单列）拖入「已选目标」
  3. 在「参数设置」中选择**算法**（逻辑回归、决策树、随机森林、K 近邻）；界面**只展示当前算法需要的参数**（例如逻辑回归显示 C、max_iter；决策树显示 max_depth、min_samples_split 等）
  4. 设置训练/测试集划分比例（如 0.3 表示 30% 测试）、随机种子（可选）；可按需启用 SelectKBest 与 k_best
  5. 点击「运行」，结果追加到统一结果窗口
- **输出**：准确率、精确率、召回率、F1；混淆矩阵（表格）；逻辑回归等可输出系数表，树模型可输出特征重要性。可选图表：混淆矩阵热力图、ROC 曲线（仅二分类）、PR 曲线（仅二分类）。

### 7.2 回归分析

- **步骤**：
  1. 菜单选择「数据挖掘 → **回归分析**」（原「线性回归」入口已统一为此名称）
  2. 在「参数设置」中选择**算法**（线性回归、Ridge、Lasso、弹性网络、决策树、随机森林）；界面**只展示当前算法需要的参数**（例如 Ridge/Lasso/弹性网络显示 alpha、max_iter；弹性网络额外显示 l1_ratio；决策树显示 max_depth、min_samples_split；随机森林显示 n_estimators、max_depth 等）
  3. 拖拽选择特征变量（X）与目标变量（y，单列）
  4. 设置测试集划分比例、随机种子；可按需启用 SelectKBest 与 k_best
  5. 点击「运行」，结果追加到统一结果窗口
- **输出**：MSE、RMSE、MAE、R²；**线性回归 / Ridge / Lasso / 弹性网络**输出系数表（列名 + 系数 + 截距）；**决策树 / 随机森林**输出特征重要性表。可选图表：预测值 vs 真实值散点图、残差图、预测分布图。

### 7.3 聚类分析

- **步骤**：
  1. 菜单选择「数据挖掘 → **聚类分析**」
  2. 仅拖拽**特征变量**（无目标变量）
  3. 在参数区选择**算法**（K-Means / 层次聚类 / DBSCAN）及对应超参（如簇数、eps、min_samples、linkage 等；界面随算法切换）
  4. 点击「运行」
- **输出**：轮廓系数等指标、各簇样本数表追加到统一结果窗口；K-Means 等会生成带「簇标签」的新结果表（命名随实现，如含 KMeans 字样）。可选图表：二维聚类散点图（按簇着色）、肘部法则曲线（仅 K-Means）、轮廓系数随 k 曲线（仅 K-Means）。

### 7.4 降维分析

- **步骤**：
  1. 菜单选择「数据挖掘 → **降维分析**」（PCA 与 t-SNE 已合并到同一入口）
  2. 在对话框「方法」中选择 **PCA** 或 **t-SNE**；**仅当前方法相关参数会显示**（PCA：主成分个数 n_components；t-SNE：perplexity；二者均使用 random_state）
  3. 拖拽**数值型**特征变量至「已选特征」
  4. 可按需勾选图表：前两维散点图、PCA 碎石图（仅 PCA）、高维结果散点矩阵（仅 PCA 且维度>=3）
  5. 点击「运行」，指标与结果表追加到统一结果窗口，并生成降维后的新表
- **输出**：样本数/特征数/降维维度等指标；PCA 附解释方差等信息表；降维坐标表（列为 PC1… 或 Dim1/Dim2）；可选图表：前两维散点图、PCA 碎石图（方差累计，仅 PCA）、高维结果散点矩阵（仅 PCA 且维度>=3）。

---

## 8. 数据可视化菜单

> ✅ **首版已上线（持续完善）**：图表工作台为主入口；描述统计/数据挖掘对话框内的快捷可选图表已接入统一结果窗口。

### 8.1 图表工作台（主入口，已上线首版）

图表工作台是一个“拖拽式图表编辑器”，主要用于：**拖拽变量 → 实时预览 → 调参 → 导出/留痕**。

- **当前版本已支持**：
  - **左侧窄栏（约 20% 宽）**：上为**参数列表（可拖拽）**，下为**选择图表类型**（下拉，含柱状、分组柱状、堆积、折线、面积、散点、气泡、回归散点、ECDF、KDE、热力、箱线、小提琴、直方、树图、桑基等）。
  - **右侧宽区（约 80% 宽）**：上为**图表预览区**；下为**参数区**（X/Y/分组/分面槽位及主操作按钮）。**图型模板**与**配置预设**（加载/保存/删除，文件 `config/chart_workbench_presets.json`）集中在窗口顶部菜单 **「模板选项」** 内（含：应用图型模板、加载/保存/删除配置预设）。**Y 槽位支持多选**；可启用**次坐标轴**并指定次轴系列。
  - 参数与导出细节：标题、轴、单位、分箱、聚合方式、max_points、标签步长、气泡缩放、DPI、导出格式、误差线/CI、显著性、子图行列等仍通过「参数设置」等入口配置。
  - **预览区交互**：左键单击可选中某根柱子/线条/点，**仅在被选中元素上右键**时弹出菜单（打开参数设置、显示数据标签、取消选中、重置视图、刷新预览）；未选中时右键为拖拽平移。**双击图表标题**可弹出编辑框修改标题并刷新预览。
  - 参数区动态灰显：切换图型后，系统会自动禁用不适用参数并给出提示（避免误配置）。

- **基本操作步骤**：
  1. 菜单选择「数据可视化 → 图表工作台」
  2. 从左侧**参数列表**拖拽变量到右下参数区的槽位（X、Y、分组、分面等）
  3. 在左下**选择图表类型**，右上方**图表预览区**会实时刷新
  4. 在右下调整样式（标题、坐标轴标签、单位、分箱、导出参数、误差线/CI、显著性标注等）
  5. 点击“导出”保存图片（支持印刷级 `300dpi/600dpi` 与 `PNG/SVG/PDF/EPS`）
  6. （可选）点击“追加到统一结果窗口”，将当前图表作为结果块保存，便于报告与回看
  7. （可选）启用“多面板子图”并设置分面槽位，可生成分面子图并自动标注 `(a)(b)...`（当前支持直方/散点/柱状/折线/箱线图）
  8. **树图**：X 槽位为类别列，Y 槽位为表示面积的数值列（按 X 聚合后绘制矩形块）；分组槽位不适用。
  9. **桑基图**：X 槽位为**来源**列，**分组槽位**为**去向**列，Y 槽位为流量数值（多行相同来源–去向对时按参数中的聚合方式合并）；桑基图不显示坐标轴刻度，左右侧为来源/去向示意文字。

- **产品与扩展说明**：工作台的设计原则（一致性、可解释的回退提示、渐进增强等）、各阶段已交付能力、后续可能增加的图型（如地理图、瀑布图等未排期项）以 **`细节实现文档` 第 10.7 节** 为准，与开发实现同步维护。

### 8.2 快捷可选图表（辅助入口，已上线）
除图表工作台外，描述统计/数据挖掘等模块对话框提供“可视化选项”，用于**运行后快速附带生成多张评估图**并追加到统一结果窗口（快捷输出，不替代工作台）。
- 当前已支持：
  - 描述统计：可选直方图 / 箱线图
  - 频率分布：可选柱状图 / 直方图
  - 相关性分析：可选相关矩阵热力图
  - 分类分析：可选混淆矩阵热力图、ROC 曲线（仅二分类）、PR 曲线（仅二分类）
  - 回归分析：可选预测值 vs 真实值散点图、残差图、预测分布图
  - 聚类分析：可选二维聚类散点图、肘部法则曲线（仅 K-Means）、轮廓系数随 k 曲线（仅 K-Means）
  - 降维分析：可选前两维散点图（PC1–PC2 或 Dim1–Dim2）、PCA 碎石图（仅 PCA）、高维结果散点矩阵（仅 PCA）

---

## 9. AI 分析菜单

> ✅ **已上线**：支持“项目级 AI 建议 → 步骤执行 → 结果回写 → 报告引用”闭环；侧栏支持 **自然语言问答（M1/M2）**、**分级可追溯展示（M3）** 与 **提示词模板白名单（M4，可选）**。

### 9.1 你可以做什么

- 在侧栏用**自然语言提问**（先在「工具」菜单顶部选择**问答范围**：已打开表 / 当前活动表），获得 **【结论】【核心依据摘要】【建议下一步】** 的简洁卡片（**M1/M2**：表用途洞见、澄清追问）。**【结论】/【建议下一步】** 行首通常会带有 **📊、✅** 等小图标（优先由云端模型写出；若未写出，软件会自动补一行首符号以便扫读）。
- **M3 可追溯**：回复卡片底部有 **「展开完整追溯」** 链接；「工具」菜单还提供 **「展开最近一条追溯…」**（独立窗口：事实结果 / 模型推断 / 推荐步骤含跳过原因 / 风险）与 **「导出本条问答追溯（JSON）」**（便于复盘）。
- 基于当前项目表结构、关系与数据摘要，生成 AI 分析建议（与问答共用底层建议结构）。
- 一键自动执行可执行步骤，或勾选步骤选择性执行。
- 执行过程在统一结果窗口显示总进度与单步状态（成功/跳过/失败）。
- 支持切换模型（本地 Mock 与自定义 Remote 模型）。
- 可导出建议内容（Markdown/HTML/JSON）并保存/恢复会话（会话 JSON 中含侧栏消息 HTML 与 **最近一次 M3 追溯记录**，便于恢复后继续导出）。

### 9.2 基本操作流程（推荐）

1. 点击主界面右侧 AI 入口，打开 AI 侧栏。  
2. 在「工具」菜单中设置**问答范围**；输入问题并**发送**（或输入传统分析目标，与问答兼容）。  
3. AI 以卡片形式返回结论与依据摘要；需要审计时**展开追溯**或导出 JSON。  
4. 若需落地：点击“自动执行”或“选择性执行”。  
5. 在统一结果窗口查看进度与结果块。  
6. 需要出报告时，在“数据报告”中勾选“AI 指导”。

### 9.3 自动执行与选择性执行

- **自动执行**：按推荐顺序批量执行可执行步骤。  
- **选择性执行**：在步骤弹窗中勾选要执行的步骤；支持按目标表筛选。  
- **失败不阻断**：单步失败会记录原因，不会中断整个执行流程。  
- **无可执行步骤时**：通常是字段不匹配、样本不足或关系待确认，请先按风险提示修正数据。

### 9.4 风险提示与可执行提示怎么看

- 风险提示会以可读文本展示（不会显示原始 JSON）。
- 每个步骤会标记是否可执行：
  - `可执行`：满足字段/类型/样本条件，可直接运行；
  - `跳过`：会给出跳过原因（如缺少字段、跨表关系需确认）。

### 9.5 模型切换与建议

- 默认可用本地规则模型（Mock），用于离线验证流程。
- 配置 Remote 后可切换到真实模型（OpenAI-compatible）。
- **一般情况下**，若 Remote 调用失败，系统会提示错误并尽量**回退到本地 Mock**，保证侧栏仍能出一份结构化建议（可能带有 `remote-fallback` 标记，属预期行为）。
- **例外**：若在 **「工具 → 提示词模板库」** 中选用了带 **「白名单」** 标识的模板，Remote 失败时 **不会** 自动用 Mock 顶替云端结果，而是**明确失败**并提示检查网络、接口与密钥，或暂时改用非白名单模板。这样可避免在「受控模板」路径下误以为已拿到完整远程结论。

### 9.6 提示词模板与白名单（M4 摘要）

- **提示词模板库**：在侧栏 **「工具」** 中打开，可选用内置或自定义模板；选用后会把模板意图一并发给模型（仍返回同一套 JSON 建议结构）。
- **白名单是什么**：一种模板标记，表示产品希望对该类问法/输出更严格。**未选用**白名单模板时，体验与过去一致；选用后，主要差别在 **Remote 不可用时的失败策略**（见 §9.5 例外说明），而不是多出完全不同的按钮或菜单。
- **如何辨认**：模板列表中带 **`[白名单]`** 字样；自定义模板可在新建/编辑时勾选「标记为白名单模板」。
- **清单与变更**：完整模板表由研发在 `开发目标.md` / `doc.md` 维护；本说明只保留用户需要理解的摘要。

### 9.7 AI 相关常见问题

- **点击执行后界面卡顿？**  
  已改为后台线程执行；如数据量很大，请观察统一结果窗口总进度条。
- **为什么有步骤却提示不可执行？**  
  可能是目标表字段不满足、目标表解析失败或跨表关系未确认；可先做预处理或在关系管理中补全关系。
- **切换模型时侧栏拉宽？**  
  现已限制模型按钮宽度并对长名称省略显示，侧栏宽度应保持稳定。
- **选了白名单模板后总是失败？**  
  请先确认 Remote 配置与网络；白名单路径**故意**不回退 Mock。可暂时换用非白名单模板验证接口，或联系管理员排查密钥与地址。

---

## 10. 帮助菜单

> ✅ **已提供基础入口**。

- **打开功能使用说明**：直接打开本地说明文档（Markdown）。
- **关于软件**：显示软件版本与定位信息。

---

## 10.1 数据报告菜单

> ✅ **V1 已上线并接入 AI 指导选项**（面向零基础的勾选模板）。

### 10.1.1 这个功能是做什么的

- 把当前表的分析结果整理成一份结构化报告。
- 不是重新计算模型，而是对已有分析内容做“可读化呈现”。

### 10.1.2 操作步骤

1. 点击主菜单「数据报告」。
2. 在“报告配置”里勾选要呈现的模块（数据概况/缺失值/描述统计/相关性/假设检验说明）。
3. （可选）勾选“AI 指导”，并选择用于报告指导的 AI 模型。  
4. 可选“自动插入图表”（若可生成则插入；不能生成时自动降级为表格与说明）。  
5. 点击“生成预览”查看排版效果。  
6. 点击“导出 HTML”保存到本地。

### 10.1.3 适合新手的内容选择建议

- 正文建议保留 3-5 个模块，避免报告过长。
- 每个模块至少配一条“结论句”，不要只贴表格。
- 若图表未生成成功，使用表格+解释同样可以得到专业报告。
- 当勾选 AI 指导时，报告会融合 AI 建议与已执行步骤证据，结构更完整。

---

## 11. 通用操作规则

### 11.1 数据输入

- 直接在表格单元格内点击输入
- 支持 Ctrl+C / Ctrl+V / Ctrl+X 复制粘贴剪切
- Delete / Backspace 清空选中单元格内容
- Ctrl+Z 撤销，支持多步撤销

### 11.2 列名输入规范

- 多列用英文逗号分隔，如：`年龄, 收入, 学历`
- 留空表示对全部适用列执行操作
- 列名必须与表格中的列头完全一致（区分大小写）

### 11.3 结果表命名规则

- 格式：`{原表名}-{操作名}`
- 示例：`表1-缺失值填充-mean`、`表1-随机抽样`

### 11.4 文件格式说明

| 格式 | 说明 |
|------|------|
| `.glc` | 软件专用格式，保存所有表格 + 列类型信息，推荐使用 |
| `.csv` | 通用文本格式，每张表单独保存为一个文件 |
| `.xlsx` | Excel 格式，每张表保存为一个 Sheet |

---

## 12. 开发维护约定

> 本节供开发人员参考，用户可忽略。

### 12.1 新增功能时必须同步更新本文档

每新增一个菜单项或功能，必须在对应章节补充：
1. 功能作用（一句话说清楚）
2. 操作步骤（按顺序列出）
3. 子选项含义（表格形式）
4. 典型使用场景
5. 注意事项/副作用

### 12.2 状态标记规范

- `✅ 已完成`：功能已实现并可正常使用
- `🔧 开发中`：功能已规划，代码开发中
- `📋 规划中`：功能已列入计划，尚未开始开发

### 12.3 与开发文档的关系

- **本文档**（`doc/Data Navigator-功能使用说明.md`）：**面向用户**，描述「怎么用」
- `doc/doc.md`：**面向开发者/架构**，描述「怎么实现」与迭代规划
- `doc/细节实现文档.md`：**面向实现与维护**，类名、信号与数据流
- 三份文档同步维护：功能状态以 `doc/doc.md` 与源码为准；用户可见行为以本文档为准

### 12.4 可视化选项与学术图表规范（用户视角）

- **默认策略**：图表生成默认关闭；当你在图表工作台或各模块的“可视化选项”中启用后，才会生成图表（可追加到统一结果窗口用于留痕/报告）。
- **设置入口**：主菜单栏「设置」。
- **已支持持久化项**（关闭软件后仍保留）：

| 设置项 | 作用 | 推荐给新手的值 |
|--------|------|----------------|
| 可视化选项默认启用 | 控制各分析对话框中“运行后追加图表”默认是否勾选 | 关闭（避免一次生成过多图） |
| 图表默认 DPI | 控制图像清晰度，值越高越清晰、文件也越大 | 日常 300；打印可 600 |
| 图表默认字体 | 下拉选择常用字体，控制图标题/坐标轴/图例字体 | Arial（中英混排稳定） |
| 图表默认导出格式 | 控制快捷图默认输出格式 | PNG（兼容性最好） |

- **对你有什么影响**：这些默认值会自动应用到预处理、描述统计、数据挖掘中的“可选图表”输出；你仍可在具体图表工作台中临时调整。

---

## 13. 版本能力核对摘要

以下为对照 `main-version.py`（截至 2026-05-08 文档整理）的结论，详细优先级见 `doc/doc.md` 与 `doc/细节实现文档.md`。

| 方向 | 状态 |
|------|------|
| 假设检验（t 检验、ANOVA、卡方、非参数等） | 已增强（单样本/独立样本/配对样本 t、ANOVA、卡方、Mann-Whitney、Kruskal + 批量单因素检验 + 可选多重比较校正） |
| 数据报告（HTML 预览/导出） | 已实现 V1（配置模板 + 预览 + HTML 导出） |
| 预处理 → 缺失值统计附带图表 | 已实现（柱状图/热力图可选追加） |
| 设置中图表默认值 | 已实现基础版（默认启用、默认 DPI、默认字体、默认格式） |
| 打印 | 已实现打印预览（大表默认前 1000 行） |
| AI 分析 / 帮助 | 已上线（侧栏建议、步骤执行；帮助打开说明与关于） |
| 底部多表切换栏 | 已优化：表多时可压窄表名按钮，「+」保持在可视区内 |

**建议产品下一迭代优先**：数据报告 V1.1（PDF、主题模板）、假设检验报告化增强（效应量/置信区间）、多表切换栏在极端数量下的「更多 ▾」或滚动、以及 `.glc` 多表写入健壮性（详见开发文档）。
- **全局默认开关（在设置里可调）**：已支持通过 `SettingsDialog` 调整「可视化选项默认启用/禁用、默认 DPI、默认字体、默认导出格式」；各分析/挖掘/预处理对话框会读取该默认值作为初始状态。
- **学术规范要求（启用后图表将满足，工作台/快捷输出通用）**：
  - 必须包含标题、坐标轴标签、图例与单位标注（适用时）。
  - 若启用误差线或置信区间（适用图表类型），需要清晰标注“误差线/CI 置信区间含义”。
  - 显著性标注（如 `* p<0.05`）可作为图表注释启用或由模块推导后给出。
  - 颜色优先使用色盲友好配色（如 ColorBrewer 风格调色板）。
- **输出与排版要求（启用后可在导出/设置中调整）**：
  - 分辨率支持印刷级（如 `300dpi/600dpi`）。
  - 导出格式支持（建议优先 `PNG/SVG`，后续扩展 `PDF/EPS`）。
  - 字体遵循要求（如 `Times New Roman/Arial`，字号 >= 10pt），并统一应用到图表元素。
- **附加增强能力（可选）**：
  - 支持分图/多面板组合（如 `(a)(b)` 子图编号）。
  - 可生成配套表格数据或补充统计/检验结果（当模块具备对应统计量时）。

---

## 14. 10 分钟快速上手

适合第一次使用 Data Navigator 的用户。按以下流程，你可以在 10 分钟内完成一次从导入到报告导出的完整体验。

### 14.1 快速流程（建议顺序）

1. 打开软件后，进入「文件 -> 打开」导入 `.csv` 或 `.xlsx`。  
2. 在主表中检查列名是否清晰，必要时双击列头改名。  
3. 进入「数据预处理 -> 缺失值统计」先看数据质量。  
4. 对缺失值较多的列，使用「缺失值填充」或「缺失值删除」。  
5. 进入「描述性统计分析 -> 描述统计」，先得到均值、标准差、分位数等基础结果。  
6. 若要比较组间差异，进入「假设检验」按向导选择检验类型。  
7. 需要图表时，打开「数据可视化 -> 图表工作台」生成并导出图。  
8. 需要总结时，点击主菜单「数据报告」，生成预览并导出 HTML 报告。  

### 14.2 新手常见建议

- 先做「缺失值统计」，再做建模或检验，能减少后续结果偏差。  
- 每一步操作都会生成新表，建议及时重命名，避免后续混淆。  
- 报告中优先保留“结论 + 关键图表 + 关键统计量”，不要只堆原始表格。  
- 需要复现时优先使用 `.glc` 保存，能保留多表与列信息。  
