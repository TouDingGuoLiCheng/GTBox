# 下一步的计划-听力考试

## 0. 背景与目标

在已有 **听力训练**（练习、即时反馈、可自定义 Do/唱名）基础上，新增 **听力考试** 模块：

- **定位**：纯测评，考完出分与报告；**不锁定、不限制**训练功能怎么用（Q1-A）。
- **与训练的区别**：考试中不显示逐题对错，全部做完才公布成绩；Do 由系统按难度策略随机指定，考生不可自选。
- **难度对齐**：入门 / 进阶 / 熟练三档，复用 `collectMidiPool`、`generateEarTrainingQuestion` 等同套难度与音高池逻辑。

**相关文档：**

- `下一步的计划-听力命名训练卡片.md` — 听力训练规则与三档难度定义（§2.3）
- `听力命名弱项分析-软件需求.md` — 练法与弱项依据

---

## 1. 你问-我回答（采访记录）

| # | 问题摘要 | 回答 |
|---|----------|------|
| 1 | 考试核心定位 | **A** 纯测评：分数/报告，不影响训练怎么用 |
| 2 | 入口位置 | **A** 首页模式菜单单独卡片（与听力训练并列） |
| 3 | 出题方式 | **随机出题**（与听力训练同类生成器），**不建固定试卷库** |
| 4 | 题量与通过线 | **A** 三级统一 **20 题/次**；正确率 **≥90%（18/20）** 算通过 |
| 5 | Do 与唱名范围 | **系统随机定 Do**（考生不选）；仅随机 **Do 音高/位置**，避免误导新手；**分级策略不同**（入门固定 Do，熟练经常换 Do） |
| 6 | 进阶档 Do 策略 | **代理决定**（见 §2.2）：20 题中 **换 1 次 Do**（约第 11 题起），介于入门与熟练之间 |
| 7 | 考中反馈 | **B** 考中不显示对错，只记录；**全部做完**才出成绩报告 |
| 8 | 考后报告 | **D** 完整报告 + 错题列表 + **可当场重做错题**（不计入本次成绩） |
| 9 | 历史记录 | **C** 保存全部历史（时间、级别、分数、是否通过），可列表翻看 |
| 10 | 时间限制 | **D** 分级不同：入门无限时；进阶/熟练加限时（具体见 §2.4，代理细化） |

---

## 2. 范围定义

### 2.1 首页入口

| 卡片 | 说明 | phase |
|------|------|-------|
| **听力考试** | 选级 → 开考 → 交卷 → 报告 | `ear-exam` |

- 布局与 `EarNamingModeMenu.vue` 现有卡片一致。
- 模块内「返回」丢弃**未交卷**的当次考试进度；已交卷报告可留在结果页查看。

### 2.2 考试流程

```
选难度（入门/进阶/熟练）→ 说明页（题数、通过线、限时）→ 开始
  → 每题：[播 Do] → 播目标音 → 选唱名（无对错提示）→ 下一题
  → 20 题结束 → 成绩报告 → [可选]错题重听/重做
```

**与听力训练共用：**

- 纯听、不显示指板。
- 唱名按钮 UI（`SolfegeAnswerPad`）；难度决定自然音 / 变化音池（`usesChromaticAnswers`、`collectMidiPool`）。
- 出题：`generateEarTrainingQuestion` + 同难度 `enabledSolfege` 默认（入门 7 自然音；进阶/熟练含变化音，与训练默认一致或可配置常量）。

**考试专有：**

| 项 | 规则 |
|----|------|
| 题数 | 固定 **20** |
| 通过 | **≥18/20（90%）** |
| 考中反馈 | **无**；仅进度「第 n/20 题」 |
| Do 指定 | **系统随机**，考生不可改 |
| 先播 Do | 考试内 **始终先播 Do**（再播目标音） |
| 训练设置 | **不读取**用户训练的 Do /「先播 Do」开关；考试独立逻辑 |

### 2.3 Do 随机策略（按难度）

Do 从该难度允许的弦范围内随机选一格（`buildFretboard` + `DIFFICULTY_STRINGS`），保证在指板可达音区内。

| 档位 | Do 变化策略 | 说明 |
|------|-------------|------|
| **入门** | **整卷 1 个 Do** | 开考随机一次，20 题不变 |
| **进阶** | **换 1 次** | 第 1～10 题 Do₁，第 11～20 题 Do₂（两格均随机且可不同把位） |
| **熟练** | **经常换** | 每 **4 题** 换一个新 Do（约 5 段：1–4、5–8、9–12、13–16、17–20）；段首第 1 题先播新 Do |

> 换 Do 时 UI 可轻提示「参照音已更新」（不透露音名），避免考生误以为设备故障。

### 2.4 时间限制（Q10-D 细化）

| 档位 | 规则 |
|------|------|
| **入门** | **无单题/整场限时** |
| **进阶** | 单题 **20 秒**内须作答，超时记错并进入下一题 |
| **熟练** | 单题 **15 秒**内须作答，超时记错并进入下一题 |

- 播放 Do + 目标音的时间 **不计入**作答倒计时（倒计时在目标音播完后开始）。
- 重播本题 / 重播 Do：考试期 **不提供**（防刷题）；仅在考后错题复习可重听。

### 2.5 考后报告（Q8-D）

**必含：**

- 总分：正确数 / 20、正确率、**是否通过**
- 用时（整场）
- 按唱名统计（如 Sol 2/2、Mi 2/3）
- 平均反应时间（仅作参考）

**错题区：**

- 列表：题号、你的答案、正确答案（唱名 + 音名）
- **重听**：按考试时快照（Do + 目标音）播放
- **重做**：单题重做，**不计入**本次考试成绩；结果可仅本地提示对错

### 2.6 历史记录（Q9-C）

- 每次交卷写入一条记录：`id`、`timestamp`、`difficulty`、`correct`、`total`、`accuracy`、`passed`、`avgResponseMs`、`bySolfege`（摘要）。
- 入口：考试模块内「历史记录」列表，按时间倒序。
- 存储：`localStorage` key 如 `earNaming.earExam.history`（与训练 settings 分离）。
- **V1 不做**：跨设备同步、图表趋势（留 V2）。

### 2.7 明确不做（首版）

- 考试结果影响训练解锁 / 功能锁定
- 固定试卷库、人工审题
- 证书导出、排行榜
- 考试中显示指板或音名提示
- 与记忆指板模块自动串联

---

## 3. 技术方案（概要）

### 3.1 状态与文件

| 路径 | 职责 |
|------|------|
| `types/earNaming.ts` | 新增 `EarNamingPhase: 'ear-exam'`、`EarExamRecord`、`EarExamPhase` 等 |
| `stores/earNamingEarExam.ts` | 考试状态机、Do 分段、计时、交卷、历史读写 |
| `utils/earNaming/earExamPlan.ts` | Do 分段计划、随机 Do 选点、20 题序列 |
| `components/earNaming/earExam/EarExamLevelMenu.vue` | 三级选择 |
| `components/earNaming/earExam/EarExamPanel.vue` | 考试中 UI（无反馈） |
| `components/earNaming/earExam/EarExamReport.vue` | 报告 + 错题重做 |
| `components/earNaming/earExam/EarExamHistory.vue` | 历史列表 |
| `EarNamingModeMenu.vue` | 新增「听力考试」卡片 |
| `EarNamingView.vue` | `phase === 'ear-exam'` 路由 |

### 3.2 复用

- `collectMidiPool`、`generateEarTrainingQuestion`、`solfegeAnswersMatch`、`playEarNamingMidi`
- `SolfegeAnswerPad`、`EAR_TRAINING_DIFFICULTY_LABELS`
- 考试每题生成时传入 **当段 doMidi**（非训练 store 的 doString/doFret）

### 3.3 考试状态机

```
idle → briefing → in-progress → submitted → report
                      ↑ 中途返回 → 丢弃（确认对话框）
```

`in-progress` 子状态：`playing-cue` | `answering` | `recording`（已选，无展示）

---

## 4. 里程碑与验收标准

### M1：入口与选级 `[x]`

**交付：**

- 首页「听力考试」卡片
- 入门 / 进阶 / 熟练三选一 + 规则说明（20 题、90% 通过、限时差异）

**验收：**

- [x] 从首页进入考试模块，三级均可进入说明页
- [x] 返回首页不崩溃；未开考可直接返回

---

### M2：考试进行（无反馈） `[x]`

**交付：**

- 20 题随机出题（同训练难度池）
- Do 分段策略（§2.3）
- 考中无对错；显示进度与倒计时（进阶/熟练）

**验收：**

- [x] 入门：20 题同一 Do，无倒计时，选答后不揭示对错
- [x] 进阶：第 11 题起 Do 变化；单题 20s 超时算错
- [x] 熟练：约每 4 题换 Do；单题 15s 超时算错
- [x] 每题先 Do 后目标音

---

### M3：交卷与报告 `[x]`

**交付：**

- 交卷页：分数、通过率、按唱名统计、错题列表
- 错题重听 + 重做（不计分）

**验收：**

- [x] 故意错 3 题，报告正确显示 17/20、未通过
- [x] 对 18 题显示通过
- [x] 错题可重听、可重做，重做不改变卷面分数

---

### M4：历史记录 `[x]`

**交付：**

- 交卷自动写入历史
- 历史列表可查看过往记录摘要

**验收：**

- [x] 连续考 2 次，列表有 2 条，含时间/级别/分数/是否通过
- [x] 刷新应用后历史仍在

---

### M5：与训练隔离 `[ ]`

**验收：**

- [ ] 考试不修改听力训练已保存的 Do、难度、唱名勾选
- [ ] 训练模块行为与加考试前一致

---

## 5. 代理决定摘要（采访未细问项）

| 项 | 决定 |
|----|------|
| 进阶换 Do | 第 11 题换 1 次 |
| 熟练换 Do | 每 4 题 1 段，共约 5 个 Do |
| 进阶限时 | 单题 20s（播完后计时） |
| 熟练限时 | 单题 15s |
| 考试先播 Do | 始终开启 |
| 唱名池 | 与各难度训练默认一致（入门 7 自然音；进阶/熟练含变化音） |
| 快捷键 | 可复用训练「空格」仅用于考后报告翻页（考中不提示对错，V1 可不做空格下一题） |

---

## 6. 风险与应对

| 风险 | 应对 |
|------|------|
| 考试与训练代码耦合过紧 | 独立 `earNamingEarExam` store；生成器只传 `doMidi` |
| 换 Do 时考生困惑 | 段首轻提示「参照音已更新」 |
| 熟练限时过严 | 常量可配置，首版 15s，用户反馈后再调 |
| 历史 localStorage 膨胀 | 上限如 200 条，超出删最旧 |

---

## 7. 立即执行清单

1. 扩展 `EarNamingPhase` + 首页卡片
2. 实现 `earExamPlan.ts`（Do 分段 + 20 题计划）
3. `earNamingEarExam` store + 考试 Panel（无反馈）
4. 报告页 + 错题重听/重做
5. 历史列表与持久化
6. 联调三档限时与 Do 策略，按 M2 验收表抽测

---

## 8. 相关文档

- `下一步的计划-听力命名训练卡片.md`
- `app/src/utils/earNaming/earTrainingDifficulty.ts`
- `app/src/stores/earNamingEarTraining.ts`
