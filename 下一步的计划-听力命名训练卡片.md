# 下一步的计划-听力命名训练卡片（V2 改版）

## 0. 改版背景与目标

V1 已完成可点击指板 + 四种「在指板上点位置」的训练模式，但与学习者真实弱项不匹配：

- **弱项核心**（见 `听力命名弱项分析-软件需求.md`）：耳朵能判断对错，但无法给声音贴唱名/音名标签。
- **V1 偏差**：练习模式侧重「标签 → 指板」记忆，缺少独立的「听音 → 选唱名」训练；UI 用 Tab 切换，不利于后续扩展新模块。

**V2 产品定位不变：**

> 不是「听力好不好」的训练器，而是「听到/想到的声音，能否立刻叫出名字、在指板上指对位置」的标签连接器。

**V2 核心改动：**

1. **UI**：五子棋式首页卡片菜单（可扩展新模块）
2. **功能拆分**：自由探索 / 听力训练 / 记忆指板 / 设置 —— 四个独立入口
3. **新增听力训练**：播音 → 选唱名（纯听，不显示指板）
4. **记忆指板**：承接现有四种指板练法，与听力训练分离

---

## 1. 你问-我回答（采访记录 · V2）

| # | 问题摘要 | 回答 |
|---|----------|------|
| 1 | 听力训练作答标签体系 | **A** 唱名（Do Re Mi …） |
| 2 | 是否先播 Do 参照音 | **B** 可选开关：有参照 / 无参照 |
| 3 | 唱名选择 UI | **A** 固定 7 个按钮全展示 |
| 4 | 出题音高范围 | **分级难度**：入门 Do～Si 无升降，进阶/熟练逐步提高 |
| 5 | 答后反馈内容 | **B** 对错 + 唱名 + 音名 |
| 6 | 首页卡片模块 | **A B C D** 自由探索、听力训练、记忆指板、设置 |
| 7 | 记忆指板 4 种子模式 | **D** 全部保留；默认突出 1～2 种，其余收在「更多」 |
| 8 | 默认突出的子模式 | **代理决定**：**A 参照音+级数定位** + **B 唱名→指板**（初学者路径） |
| 9 | 首版范围 | **D** 全做完：UI 重构 + 听力训练完整 + 记忆指板打磨 + 统计 + 错题复习 + 设置打通 |
| 10 | 听力训练是否显示指板 | **A** 完全不显示指板 |
| 11 | 难度三档定义 | **A** 同意入门/进阶/熟练三档（见 §2.3） |
| 12 | 错题复习 | **A** 每轮结束后列出错题，可重听并重做 |
| 13 | 设置共用方式 | **C** 各模块独立配置；设置卡片仅为入口汇总 |
| 14 | 音频控制 | **A** 重播本题 + 重播 Do 参照（有参照模式时） |
| 15 | 中途返回首页 | **A** 直接丢弃本轮，回首页重选模块 |

---

## 2. 范围定义（V2）

### 2.1 首页卡片（对标五子棋 `GomokuModeMenu`）

进入「听力命名训练」工具后，默认展示卡片网格，而非 Tab：

| 卡片 | 说明 | 子页面 |
|------|------|--------|
| **自由探索** | 点击指板发声，查看音名/唱名/级数，同音异位 | `phase: explore` |
| **听力训练** | 播音 → 选唱名，无指板 | `phase: ear-training` |
| **记忆指板** | 标签/级数/音程 → 在指板点位置 | `phase: fretboard-memory` |
| **设置** | 各模块配置入口汇总（非全局强制统一） | `phase: settings` |

- 卡片布局参考 `GomokuModeMenu.vue`：`ui-card` 网格、hover 动效、图标 + 主标题 + 副标题
- 各模块内「返回」回到卡片首页；**中途返回丢弃本轮进度**（Q15-A）
- 预留占位格或虚线格，便于 V3 插入新模块（如移调、曲目扒音）

### 2.2 听力训练（P0 新增 · 核心）

**流程：**

```
开始本轮 → [可选]播 Do 参照 → 播目标音 → 用户点唱名按钮 → 反馈(对错+唱名+音名) → 下一题 → 本轮结束 → 错题回顾
```

**规则：**

- 作答：7 个固定唱名按钮（Do Re Mi Fa Sol La Si）
- 页面：**不显示指板**（Q10-A）
- 参照音：设置内开关「有参照 / 无参照」（Q2-B）
- 音频：「重播本题」「重播 Do」（有参照时）（Q14-A）
- 反馈：对错 + 正确答案唱名 + 音名（Q5-B）；**不展示指板位置**（与「纯听」定位一致，指板关联留给记忆指板模块）
- 出题：基于当前模块内 Do 参照（默认 6 弦 8 品 = C，可在本模块设置中改）
- 每轮题数：5 / 10 / 15（本模块设置）
- 统计：正确率、平均响应时长、按唱名/级数错误分布
- 错题复习：本轮结束后列出错题，支持重听原题音频并重做（Q12-A）

### 2.3 听力训练 · 难度三档（Q11-A）

| 档位 | 音高范围 | 出题策略 |
|------|----------|----------|
| **入门** | C 大调自然音 Do～Si，无升降号 | 音域偏吉他 3～6 弦常用区（midi 范围可配置常量） |
| **进阶** | 自然音 + 少量变化音（Fi、Ra 等唱名扩展） | 音域扩大至 2～6 弦常用区 |
| **熟练** | 含升降号、更大音域 | 可出变化音唱名；同轮内避免连续相同唱名 |

> 注：熟练档 **V2 不做八度区分**（不高/低 Do 分栏），避免初学者认知过载；留 V3。

### 2.4 记忆指板（承接 V1 训练逻辑）

- 保留四种子模式：级数定位、唱名找位、两音音程、命名扒音
- **默认主屏展示**：参照音+级数定位、唱名→指板（Q8，初学者路径）
- **「更多」收起**：两音音程、命名扒音
- 交互沿用 V1 指板点击判题 + 右侧反馈 + 同音异位
- 子模式选择页：二级卡片或列表，风格与首页一致
- 本模块独立设置：Do 参照、练习弦范围、每轮题数、扒音段长等（Q13-C）

### 2.5 自由探索（V1 保留，入口调整）

- 从 Tab 改为卡片进入
- 功能不变：22 品指板、点击发声、标签显示、同音异位开关
- 无轮次/进度概念

### 2.6 设置（入口汇总 · Q13-C）

设置卡片打开后，分区块跳转或折叠面板：

- 自由探索设置（如：同音异位默认、悬停标签）
- 听力训练设置（难度、参照音开关、题数、Do 参照）
- 记忆指板设置（Do 参照、弦范围、默认子模式、题数）
- **各模块配置独立存储**，互不强制覆盖

### 2.7 明确不做（V2）

- 听力训练过程中显示指板（含只读指板）
- 全局强制统一所有模块的 Do/弦范围（用户选了分模块管理）
- 跨轮次持久化错题本（仅当轮复习，Q12-A）
- 麦克风哼唱检测（V3）
- 中途返回保留进度（Q15-A）

---

## 3. 功能架构设计（V2）

### 3.1 页面相位（替代 `pageTab`）

```ts
type EarNamingPhase =
  | "menu"              // 首页卡片
  | "explore"           // 自由探索
  | "ear-training"      // 听力训练
  | "fretboard-memory"  // 记忆指板（含子模式选择 / 训练中）
  | "settings";         // 设置汇总
```

`EarNamingView.vue` 参考 `GomokuView.vue`：`phase === 'menu'` 渲染 `EarNamingModeMenu`，否则渲染对应子视图。

### 3.2 新增 / 调整文件

| 文件 | 用途 |
|------|------|
| `components/earNaming/EarNamingModeMenu.vue` | 首页四卡片（对标 GomokuModeMenu） |
| `components/earNaming/EarNamingSettingsHub.vue` | 设置入口汇总 |
| `components/earNaming/earTraining/EarTrainingPanel.vue` | 听力训练主面板 |
| `components/earNaming/earTraining/SolfegeAnswerPad.vue` | 7 唱名作答区 |
| `components/earNaming/earTraining/EarTrainingFeedback.vue` | 听训反馈 + 音频控制 |
| `components/earNaming/earTraining/WrongAnswerReview.vue` | 当轮错题回顾 |
| `components/earNaming/fretboard/FretboardModeMenu.vue` | 记忆指板子模式入口 |
| `components/earNaming/fretboard/FretboardTrainingPanel.vue` | 从 V1 训练区拆分 |
| `stores/earNamingEarTraining.ts` | 听力训练独立状态（或 store 内分区） |
| `utils/earNaming/earTrainingGenerator.ts` | 按难度档生成听训题目 |
| `utils/earNaming/earTrainingDifficulty.ts` | 三档难度常量与 midi 池 |

### 3.3 数据模型补充

```ts
type EarTrainingDifficulty = "beginner" | "intermediate" | "advanced";

interface EarTrainingPrompt {
  questionIndex: number;
  totalQuestions: number;
  targetMidi: number;
  targetSolfege: string;   // 正确答案唱名
  targetNoteName: string;  // 正确答案音名
  useDoReference: boolean;
  doMidi: number;
  difficulty: EarTrainingDifficulty;
}

interface EarTrainingAnswerEvent {
  pickedSolfege: string;
  correct: boolean;
  responseMs: number;
  // ...用于当轮错题回顾
}
```

### 3.4 学习路径（初学者推荐）

```mermaid
flowchart LR
  A[自由探索<br/>熟悉指板音高] --> B[听力训练·入门<br/>听音选唱名]
  B --> C[记忆指板·级数定位<br/>Do参照找级数]
  C --> D[记忆指板·唱名找位<br/>唱名映射多位置]
  D --> E[更多模式<br/>音程 / 扒音]
```

---

## 4. 页面交互草案

### 4.1 首页卡片

- 2×2 或 3 列响应式网格（与五子棋一致）
- 每卡：图标 + 标题 + 一句副标题
- 可选：设置卡放左上角或末位（参考五子棋设置位）

### 4.2 听力训练页

```
┌─────────────────────────────────────┐
│ ← 返回    听力训练    [本轮 3/10]    │
├─────────────────────────────────────┤
│  🔊 正在播放…  [重播本题] [重播Do]   │
│         （波形/大播放按钮区）         │
├─────────────────────────────────────┤
│  Do  Re  Mi  Fa  Sol  La  Si        │
│         （7 唱名作答按钮）            │
├─────────────────────────────────────┤
│  反馈：✓ Mi · E   或  ✗ 你选 Fa，正确 Mi · E │
└─────────────────────────────────────┘
（无指板）
```

### 4.3 记忆指板页

- 先进子模式菜单：主屏 2 卡（级数定位、唱名找位）+「更多」展开 2 卡
- 训练页布局同 V1：顶栏 + 指板 + 右侧反馈

### 4.4 本轮结束

- 统计摘要（正确率、弱项唱名）
- **错题列表**：每题可「重听 + 重做」
- 按钮：「再练一轮」「返回首页」

---

## 5. 开发里程碑与验收标准

说明：每个阶段完成后，将状态标为 `[x]`，未完成保持 `[ ]`。

### M1：首页卡片 + 模块路由拆分

状态：`[x]`

交付项：

- `EarNamingModeMenu` 四卡片首页
- `EarNamingPhase` 状态机；各模块可进入/返回
- 自由探索从 Tab 迁入独立 phase
- 中途返回丢弃本轮（听力/记忆指板）

验收标准：

- 进入工具默认见卡片首页，风格与五子棋模式菜单一致
- 四卡片均可进入且可返回首页
- 训练中点返回，再进入同模块为新轮/空闲态

### M2：听力训练完整闭环

状态：`[x]`

交付项：

- 播音 → 7 唱名作答 → 反馈（唱名+音名）
- 有/无 Do 参照开关
- 重播本题、重播 Do
- 难度三档（入门/进阶/熟练）及出题池
- 页面无指板

验收标准：

- 入门档连续完成 10 题无中断
- 有参照模式：每题可先 Do 再目标音；无参照模式：仅目标音
- 反馈仅含对错+唱名+音名，不出现指板
- 三档难度题目池与 §2.3 一致（抽测 20 题）

### M3：记忆指板模块打磨

状态：`[x]`

交付项：

- 子模式二级菜单：默认突出级数定位 + 唱名找位，「更多」含音程+扒音
- V1 训练逻辑迁入 `fretboard-memory` phase
- 本模块独立设置项

验收标准：

- 四种模式均可完整跑 10 题
- 主屏仅显示 2 个推荐模式，「更多」可展开另外 2 个
- 与听力训练状态隔离，切换模块不串题

### M4：设置汇总 + 分模块存储

状态：`[x]`

交付项：

- 设置卡片 + 分模块配置面板
- 各模块读写独立 settings key（localStorage 或现有持久化方案）
- 听力训练/记忆指板 Do 参照可各自配置

验收标准：

- 在听力训练改 Do 参照，不影响记忆指板已保存的 Do（除非用户在该模块也改）
- 设置页可到达三个子模块的配置区

### M5：统计 + 当轮错题复习

状态：`[x]`

交付项：

- 听力训练、记忆指板分别输出本轮统计
- 本轮结束错题列表 + 重听重做
- 弱项提示（如「Mi / 3 级错误偏多」）

验收标准：

- 故意错 3 题后，结束页列出 3 题且可重听音频
- 错题重做不计入原轮统计（或分开展示，需在 UI 标明）
- 至少 1 条可解释的弱项提示

---

## 6. 成功指标（与需求文档对齐）

| 指标 | 基线 | V2 软件内目标 |
|------|------|----------------|
| 听单音选唱名（入门档 8 题） | 未系统练 | ≥ 6/8 |
| 随机找音（记忆指板·唱名模式） | 2/8 | ≥ 6/8 |
| 参照音+级数 4 题 | 未稳定 | 4/4 |
| 两音音程 | 未练 | 8 题 ≥ 6/8（更多模式） |

---

## 7. 风险与应对

| 风险 | 应对 |
|------|------|
| 听力训练与记忆指板概念混淆 | 卡片文案区分：「听音选唱名」vs「唱名找位置」 |
| 分模块设置导致 Do 不一致 | 设置页注明「各模式独立」；首次进入给推荐默认值 |
| V1 Tab/训练代码拆分工作量大 | 先抽 phase 路由，再渐进迁移组件，避免大爆炸重写 |
| 进阶档变化音唱名不统一 | 固定 Fi/Ra 与升降号映射表，UI 图例说明 |

---

## 8. V1 里程碑（已完成 · 存档）

- M1 可点击发声指板 `[x]`
- M2 P0 三模式训练闭环 `[x]`
- M3 扒音 + 统计看板 `[x]`

V1 能力作为「记忆指板」模块底层复用，不废弃。

---

## 9. 立即执行清单

1. 实现 `EarNamingModeMenu` + phase 状态机（M1）
2. 新建听力训练面板与出题器（M2）
3. 拆分记忆指板子模式菜单（M3）
4. 设置汇总与分模块持久化（M4）
5. 当轮错题复习（M5）

---

## 10. 相关文档

- `听力命名弱项分析-软件需求.md` — 弱项与练法依据
- `app/src/components/gomoku/GomokuModeMenu.vue` — UI 参考
- `app/src/views/EarNamingView.vue` — 当前待重构入口
