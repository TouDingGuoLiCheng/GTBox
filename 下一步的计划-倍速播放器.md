# 下一步的计划-倍速播放器

## 0. 背景与目标

学习吉他时通过 B 站跟弹教学视频，但 B 站自带倍速只有 **0.5 / 0.75 / 1.0** 等档位，**0.75 与 1.0 之间无法细调**，难以循序渐进练到原速。

新增工具 **倍速播放器**：

- **定位**：在工具箱内嵌打开视频网站（首版 **B 站**），正常使用网页浏览与 **B 站自带登录**；插件仅对 **当前正在播放的视频** 叠加倍速与练习辅助能力。
- **核心价值**：滑块连续倍速（如 0.80、0.85、0.90），填补 0.75→1.0 的练习空白。
- **扩展性**：首页为「视频网站卡片」入口，二期可加 YouTube 等站点卡片。

**相关约束：**

- 遵循根目录 `开发规范.md`：界面从简、图标+tooltip、调试日志 `pushDebugLine`、复用现有 Tauri/Vue 模式。
- 可参考现有翻译模块的 **Webview + `eval` 注入** 模式（`translate/window_util.rs`、`translator.rs`）。

---

## 1. 你问-我回答（采访记录）

| # | 问题摘要 | 回答 |
|---|----------|------|
| 1 | 视频来源 | **A** 内嵌打开 B 站等网页，叠加自定义倍速 |
| 2 | 支持站点 | **A** 先 B 站；架构预留扩展其他站 |
| 3 | 倍速调节方式 | **A** 滑块连续调节 |
| 4 | 登录 | **B** 使用 **B 站网页自带登录**（扫码/账号），登录态保存在内嵌环境 |
| 5 | 打开视频方式 | **正常用 B 站** 找片、播放；工具 **只控制当前视频倍速**，不做独立纯播放器 |
| 6 | 控制面板位置 | **A** 悬浮条，叠在视频区域上/下 |
| 7 | 除倍速外功能 | **A+B+C** + **时间节点标记与快捷键跳转**；悬浮条 **支持鼠标操作** |
| 8 | 变调处理 | **与 B 站一致**：浏览器原生 `playbackRate`，自然变调 |
| 9 | 工具箱入口 | **A** 首页新工具；内部为 **视频网站卡片主页**，点卡片进对应站点 |
| 10 | 标记持久化 | **B** 按视频（BV 号）保存，下次打开同一视频恢复 |
| 11 | 快捷键 | **C** 第一版固定快捷键，后续支持自定义 |
| 12 | MVP 范围 | **1～6 必做**；**7（其他视频网站）二期** |
| 13 | 倍速范围与精度 | **A** **0.25～2.0**，步进 **0.05** |
| 14 | 工具命名 | **A** **倍速播放器** |
| 15 | 验收标准 | **A+B+C** 全流程可用 + 全功能验收 + 30 分钟稳定性 |

---

## 2. 范围定义

### 2.1 页面结构

```
工具箱首页「倍速播放器」卡片
  → 视频网站卡片主页（V1 仅 B 站一张卡）
    → 内嵌 B 站全站 WebView（浏览、搜索、登录、点播与现网一致）
      → 检测到视频播放页时，显示悬浮控制条
```

| 页面 | 说明 | phase |
|------|------|-------|
| **站点选择** | B 站卡片（二期加 YouTube 等） | `site-picker` |
| **站点浏览** | 内嵌 `https://www.bilibili.com` 起始于首页或上次 URL | `site-bilibili` |

- 模块内「返回」：从 B 站页回到站点卡片主页；从站点主页回到工具箱首页。
- **不改造** B 站页面布局；暂停、进度、音量等继续用 B 站自带控件。

### 2.2 悬浮控制条（必做）

叠在视频区域 **底部**（可拖动、可收起），鼠标可操作：

| 控件 | 行为 |
|------|------|
| 倍速滑块 | 0.25～2.0，步进 0.05；旁显示当前倍速数字 |
| ± 按钮 | 可选快捷 ±0.05（与滑块联动） |
| AB 循环 | 设 A/B 点，循环播放片段；可清除 |
| 打标记 | 在当前进度打一个时间节点 |
| 标记列表 | 显示已打标记，点击跳转；支持删除 |
| 收起 | 仅留小把手，减少挡画面 |

**倍速生效规则：**

- 仅作用于 **当前页 `<video>` 元素**（含 B 站播放器切换清晰度、切 P 后需重新绑定）。
- 若 B 站 UI 显示自带倍速，以 **本工具滑块为准**；注入后需 **监听 DOM / 播放器事件**，防止被站方重置（见 §3.3 风险）。

### 2.3 AB 循环

- **A 点 / B 点**：记录当前播放时间；B 未设时默认到视频末尾。
- 循环时 `currentTime` 到达 B 后跳回 A。
- 与倍速、标记独立；关闭 AB 或离开视频页时清除当次 AB 状态（**不持久化**，除非后续需求变更）。

### 2.4 时间节点标记

- 在当前进度插入标记（**自定义名称**；默认「标记 n」）；**双击**可重命名。
- **按 BV 号（或 av 号）持久化**到本地（`localStorage` on bilibili.com）。
- 离开视频页再进入同一 BV，标记列表恢复。

### 2.5 快捷键（V1 固定，文档列出）

建议默认值（实现时可微调，写入帮助 tooltip）：

| 快捷键 | 功能 |
|--------|------|
| `[` / `]` | 倍速 −0.05 / +0.05 |
| `Shift+A` / `Shift+B` | 设 AB 点 |
| `Shift+L` | 开关 AB 循环 |
| `M` | 当前进度打标记 |
| `1`～`9` | 跳转到第 1～9 个标记（不足则忽略） |
| `Esc` | 收起/展开悬浮条 |

- 焦点在 WebView 内时仍需响应：通过 **Tauri 全局快捷键** 或 **注入页面的 `keydown` 监听**（优先不与 B 站常用键冲突）。
- **二期**：设置页自定义键位。

### 2.6 明确不做（首版）

- 其他视频网站卡片（YouTube 等）— **二期**
- 快捷键自定义 UI — **二期**
- 标记导出/备份、跨设备同步 — **二期**
- 本地视频文件播放 — **不做**（与采访结论一致）
- 保音高变速算法 — **不做**（跟随 B 站原生听感）
- 下载视频、去广告、破解会员 — **不做**

---

## 3. 技术方案（概要）

### 3.1 整体架构

```
Vue 壳（SpeedPlayerView）
├── SitePicker.vue          # 视频网站卡片
├── SiteWebviewShell.vue    # 布局：WebView 区域 + 悬浮条（Vue 层，在 WebView 之上）
├── SpeedControlOverlay.vue # 滑块 / AB / 标记 UI
└── composables/
    ├── useSpeedPlayerInject.ts   # 与 WebView 通信
    ├── useSpeedPlayerMarkers.ts  # 标记读写（按 videoId）
    └── useSpeedPlayerHotkeys.ts  # 全局快捷键

Tauri
├── speed_player.rs           # WebView 创建、导航、eval 注入
└── 复用 translate/window_util 模式
```

**WebView 策略：**

- 为 B 站创建 **持久 label** 的 `WebviewWindow` 或 **子 WebView**（嵌入主窗口），`url` 初始为 `https://www.bilibili.com`。
- **独立 partition / data directory**（若 Tauri 支持），保证 B 站 Cookie 与主应用隔离且可长期保存。

### 3.2 视频检测与倍速注入

注入脚本（周期性 + 事件驱动）：

1. 解析当前页 **BV/av 号**（`window.__INITIAL_STATE__` 或 URL）。
2. 查找页面内 `video` 元素，设置 `video.playbackRate = targetRate`。
3. 向 Vue 层 `postMessage` / Tauri `emit`：当前时间、`duration`、是否暂停、videoId。
4. AB 循环、跳转标记在注入脚本或 Vue 侧根据 `timeupdate` 驱动。

参考：`app/src-tauri/src/translate/translator.rs` 中 `eval_inject_*` / `run_eval_script` 模式。

### 3.3 风险与对策

| 风险 | 对策 |
|------|------|
| B 站换播放器或 Shadow DOM，找不到 `video` | 多选择器回退；`MutationObserver`；调试日志记录失败原因 |
| 站方重置 `playbackRate` | `timeupdate` 时若偏离目标则写回；用户可见当前实际倍速 |
| WebView 内快捷键被网页吃掉 | Tauri 全局 shortcut + 注入 `capture` 阶段监听 |
| 音画不同步（极高倍速） | V1 不承诺 >2.0；0.25～2.0 范围内以可接受为准 |
| B 站反爬 / 登录风控 | 使用官方域名完整页；不篡改请求；失败时提示用浏览器 |

### 3.4 数据存储

| Key / 文件 | 内容 |
|------------|------|
| `speedPlayer.markers.{bvId}` | `{ id, timeSec, label? }[]` |
| `speedPlayer.lastUrl.bilibili` | 上次浏览 URL（可选） |
| `speedPlayer.lastRate.{bvId}` | 上次倍速（**可选增强**，采访未强制，实现有余力可加） |

### 3.5 注册入口

| 路径 | 职责 |
|------|------|
| `mockTools.ts` | 新增 `speed_player` 卡片 |
| `router/index.ts` | `/tools/speed-player` |
| `views/SpeedPlayerView.vue` | 主视图 |
| `components/speedPlayer/*` | 子组件 |
| `src-tauri/src/speed_player.rs` | WebView 与注入命令 |
| `src-tauri/src/lib.rs` | 注册 command |

---

## 4. 里程碑与验收标准

### M1：入口与 B 站内嵌 `[ ]`

**交付：**

- 首页「倍速播放器」卡片
- 站点卡片主页（V1 仅 B 站）
- 点击进入内嵌 B 站，可浏览、搜索、打开视频页
- B 站网页登录可用，重启应用后登录态仍在

**验收：**

- [ ] 从首页进入倍速播放器，再进入 B 站页
- [ ] 扫码/账号登录 B 站成功
- [ ] 关闭并重启工具箱，仍保持登录（同分区 Cookie）
- [ ] 返回链：B 站页 → 站点主页 → 工具箱首页

---

### M2：连续倍速悬浮条 `[ ]`

**交付：**

- 视频播放页出现悬浮条
- 滑块 0.25～2.0，步进 0.05
- 鼠标可调；`[` `]` 微调
- 实际听感与变速生效（含 0.80、0.85、0.90 等中间档）

**验收：**

- [ ] 打开吉他教学视频，调到 **0.85** 可跟弹，明显不同于 0.75 与 1.0
- [ ] 拖动滑块与快捷键同步更新显示
- [ ] 切换清晰度 / 分 P 后倍速仍生效或可快速恢复
- [ ] 悬浮条可收起，不太挡谱面/指法画面

---

### M3：AB 循环 `[ ]`

**交付：**

- Shift+A / Shift+B 设点，Shift+L 开关循环
- 悬浮条按钮可设 AB、清除

**验收：**

- [ ] 选定 10～30 秒片段，循环播放稳定，无累计漂移（容差 ±0.2s）
- [ ] 关闭 AB 后正常整段播放

---

### M4：标记与跳转 `[ ]`

**交付：**

- `M` 打标记；`1`～`9` 跳转；列表点击跳转
- 按 BV 持久化

**验收：**

- [ ] 同一视频打 3 个标记，刷新页面或次日再开，标记仍在
- [ ] 快捷键与鼠标点击均可跳转
- [ ] 换另一个 BV 视频，标记列表互不干扰

---

### M5：稳定性与整体验收 `[x]`

**交付：**

- 倍速防重置：`timeupdate` / `ratechange` / 250ms 轮询写回；悬浮条显示纠正次数
- Shadow DOM 内 `video` 探测；切 P / 换源后自动重绑
- **按 BV 记忆倍速**；再次打开恢复上次浏览页（非首页时）
- 调试：`speed_player_diagnostics` + 设置里调试终端可见 `倍速播放器` 日志
- 主窗快捷键转发（焦点不在 B 站时 `[` `]` 等仍可用）
- AB **清AB** 按钮

**验收（对应 Q15 A+B+C）：**

- [ ] **A** 完整练琴流程（需你在客户端实测）
- [ ] **B** AB、标记、快捷键、持久化（需实测）
- [ ] **C** 连续使用 **≥30 分钟** 无崩溃（需实测）

---

### M6（二期）：多站点扩展 `[ ]`

**交付：**

- 站点卡片增加新站点（如 YouTube）
- 各站独立 WebView 分区与注入适配器

**验收：**

- [ ] 新站点卡片可进入并完成与 M2 同等的倍速控制

---

## 5. 建议实现顺序

1. **M1** — WebView 壳 + 路由 + B 站能逛能登
2. **M2** — 注入 `playbackRate` + 悬浮条（先解决核心痛点）
3. **M3** — AB 循环
4. **M4** — 标记持久化 + 跳转
5. **M5** — 抗重置、热键打磨、长时稳定性
6. **M6** — 二期多站点

---

## 6. 已确认选项

- 悬浮条默认在视频 **下方**
- 标记支持 **自定义名称**（打标记时输入；双击重命名）

---

*文档版本：采访 15 问完成后生成；M1 开发已启动。*
